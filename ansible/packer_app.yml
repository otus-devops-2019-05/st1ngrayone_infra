---
- name: Install Ruby
  hosts: default
  gather_facts: no
  become: true
  tasks:
    - name: Install a list of packages
      apt:
        name: "{{ packages }}"
        update_cache: yes
      vars:
        packages:
        - ruby-full
        - ruby-bundler
        - build-essential
